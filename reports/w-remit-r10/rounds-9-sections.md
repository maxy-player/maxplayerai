# Round 9 — N1–N4 in full (moved out of the PR body, round 10)

Moved verbatim out of `body-r10.md` on 2026-09-08 to keep the pinned body under GitHub's 65,536-byte cap, with the
round-10 sections still to be added. Nothing here is rewritten: this is the round-9 text as it stood at the `1ee5cb2`
pin, carrying the round-10 correction the verdict's §7.2 H2 required (record 39's `fee_paid` is 5, not 4). The body
keeps a pointer, the two corrections, and the round-9 prior-finding ledger.

**N1 / B — one bound, the SDK's, taken where the money is (addendum 10 §1; verdict §3.2).** Round 8 had two gates in series — the wallet's `admits_total` on the SDK's *prepared* figures, then the caller's `confirm_would_succeed` on the *post-swap* recomputation — and its planner accepted the prepared figures, so a gross of 19 whose invoice 13 confirms was refused by the prepared-total early return while the helper had already found it (N1). Now there is **one** bound and it is the confirm arithmetic: `confirm_bound` (`wallet_ops.rs:396`) and `MeltCeiling::admits_confirmable` (`:479`; `ConfirmBound` `:348`, `total_debit` `:507`) — target = invoice + reserve + prepared input fee, `binary_split(target)` (`:318`), actual fee `fee_for(ppk, len)` (`:311`), admitted only if target ≥ invoice + reserve + actual fee and invoice + reserve + actual fee + swap fee ≤ gross. The wallet thread reads the active keyset's `input_fee_ppk` **before** the gate (`:1709`; `active_keyset_input_fee_ppk` `:1533`) and runs the gate at `:1733` — a shortfall cancels the preparation (`prepared.cancel()` `:1782`) and returns the typed refusal (`MeltExceedsCeiling` / `MeltWouldNotConfirm` / `MeltTotalExceedsCeiling`, `:56` / `:89` / `:73`); `admits_total` is gone (census: 0 added lines). The planner runs the same bound (`plan_confirmable_invoice` `fee_remit.rs:2431`) and the prepared-total early return is removed (commit B); the caller-side `confirm_would_succeed` (`:2470`, at `:1785`) stays as the check on the figures the wallet handed back. **Regressions:** `a_fee_bearing_payment_at_a_19_sat_gross_the_prepared_estimate_would_refuse_pays_invoice_13_once` (`:5155`; record 39 at `fee_remit.rs:5454` — gross 19 = `[10, 9]`, reserve 2, 1000 ppk, pool `[32]`: invoice **13**, one swap, one melt, pool 32 → 12 + 1 = 13, delta 19 ≤ 19, **`fee_paid` 5**, no WARNING. The round-9 body wrote `fee_paid` 4 next to delta 19; those two are inconsistent and 4 was the wrong one — the fixture returns `melt_results = vec![Ok((13, 2))]`, the test asserts `(*net_sats, *melt_fee_sats) == (13, 5)`, and the emitted line reads “melt fee taken by the mint: 5 sats”: 5 = Lightning 2 + actual proof input 3, so 13 + 5 + 1 change = 19, the gross exactly (verdict §7.2 H2, re-read at 08c476c)); the `confirm_bound` table `the_confirmability_bound_selects_the_verdicts_invoices_when_searched_downward` (record 42: 19/2 ⇒ 13, 20/0 ⇒ 15, 20/2 ⇒ 13, 3/1 ⇒ none); `a_melt_ceiling_admits_a_prepared_melt_by_what_confirm_will_actually_debit` (record 43, the renamed record 33). **Same-attempt re-plan (addendum 10 §1.4):** when the payment quote's live reserve is **smaller** than the estimate's, the planned invoice is re-planned once against the live figures before anything is prepared — `SellerStore::replan_remittance` (`store.rs:2287`; one conditional `UPDATE` on our own Planned, unbound row, receipts kept pinned, zero rows ⇒ refuse) called at `fee_remit.rs:1692`, one `Re-planned:` line (`:1715`); a second mismatch is not re-planned again, and a **grown** reserve is refused as before (record 14, `:5593`). Regressions: `a_reserve_that_shrinks_between_estimate_and_payment_is_re_planned_once_and_pays_invoice_15` (`:5019`; record 40 — 20 / est 3 / live 0: plan 12 → re-plan 15, one `Re-planned:` line, invoice 15 paid once, pool 32 → 13, delta 19, one row Settled, `net_sats == 15`, `melt_quote_id` = the second quote) and `replan_remittance_updates_only_our_own_planned_unbound_row_and_keeps_receipts_pinned` (record 41; zero rows when Spending or another owner). Because the re-plan happens before `prepare`, nothing is cancelled and the row is only updated, never released; gross, the post-confirm hold and every ownership control are untouched.

**N2 / B — a pre-fence arithmetic refusal writes nothing and prints one line (addendum 10 §2; verdict §3.3).** `refuse_before_fence` (`fee_remit.rs:1525`) no longer releases the row: the row **stays Planned**, unbound (`spending_since_unix IS NULL`, `spending_quote_id IS NULL`), owner and lease intact, receipts pinned; the attempt journal records `Failed`; the output is exactly one line (`:1538`: `REFUSED before spending — {reason}; ceiling {gross} sats; nothing left the wallet; the row stays planned and the next attempt re-quotes.`); cancel precedes the line (`:1788`). The next attempt's reconciliation releases the row as its owner's earlier attempt and re-plans under N1 — "backoff continues and the next attempt re-quotes and re-plans", the ordered contract. `release_own_planned` (`:1481`) remains for the two non-arithmetic callers (quote failure, lost fence with too little lease). Re-asserted with the shared helper `assert_refused_before_fence_row_stays_planned` (`:3595`: one `REFUSED before spending —` line, row Planned, unbound, receipts 2, planned quote id): records 37 (`:5284`, on its drift schedule — §5 above), 29 (`:5384`) and 14 (`:5593`, whose second attempt now shows the reconcile-release line first and then a new row pays: rows `[Failed, Settled]`); financial expectations unchanged (delta 0, no swap, quote UNPAID).

**N3 / G — the reserve bounds the Lightning fee, and the labels say so (addendum 10 §3; verdict §4).** Source: `wallet_ops.rs:292–295` (`MeltOutcome::fee_reserve_sats`), `:528–531` (`MeltEstimate`); the PAID-reconciliation line prints `Lightning fee at most {reserve} sats (the quote's reserve)` and records the inclusive melt fee as not observed (`fee_remit.rs:1105`, `fee = None`); the plan line labels `mint melt fee reserve (bounds the Lightning fee)` and names reserve · estimated proof input fee · actual proof input fee · swap fee · inclusive melt fee apart (`:1386`, `:1392`). Asserted by `interrupted_after_the_plan_is_reconciled_as_paid_without_a_second_melt` (`:4067`; record 44, new: the line names `Lightning fee at most` and not `melt fee at most`) and the plan-line assertions of records 28 and 30. Commit `2521a44`.

**N4 / H — the eight contradictions, each closed where it lived (verdict §6).** (1) three cancel sites, not two — §5 line above (`:1788`, `:1841`, `:1892`); (2) prepare is not "local only" — §5 line and the F3 paragraph above (`keysets.rs:44–51` → `mint_metadata_cache.rs:366`), and the false "every phrase replaced" claim is named there; (3) record 28 prepared **4** ≠ actual 3 — the money-hold line above; (4) record 37's refusal restated on the ordered contract with prepared total (16) and confirm target (15) named apart — the F1 paragraph above; (5) the true order reserve-only precheck → re-plan → prepare → post-swap arithmetic → fence → confirm — F1 paragraph and the spend-order sentence above; (6) `from_prepared` zeroes `swap_fee` unconditionally (`saga/mod.rs:592`) — F1 paragraph above; (7) = N3 (source `2521a44`; body above); (8) the store fence doc says HELD until PAID (`store.rs:2199–2202`, `2521a44`) — the hold paragraph above. Every citation in those sentences was re-read at `240240c` by hand while writing, and the whole body is re-anchored and read back below (`citation-remap-240240c.txt`, `citation-selfcheck-240240c.txt`), then re-pinned.

**Round 9 commits, in order** (`git log --format=%s`; every one authored and committed as `w-seller-fee-stage2a-remit <worker@forge.local>`): `b0c2c03` (plan) → `c9cce6c` (A) → `eefdb70` (B) → `b7e3f34` (C) → `573ab50` `c06b490` `1f03898` `bcbc8b9` (D) → `fa2b8af` → `e6f7b7a` → `380f097` → `9ae7c9c` `71b4971` (E) → `2521a44` (F) → `240240c` (lint fix-up — **the executable head**), then reports only; what each did, one line per commit, is in [`commit-history-a6217328-240240c.md`](https://github.com/maxy-player/maxplayerai/blob/293e0e6a258f61cd1c79e0c2fb82e5afbfaaf26d/reports/w-remit-r9/commit-history-a6217328-240240c.md) (the CI section below reads every one of these heads).
