Round 10 step 5 — figures RE-MEASURED at exec head 08c476c (never copied from an order or from r9).
Commands are literal; run them at 08c476c to reproduce.

H4 — added set
  git diff --numstat a6217328..08c476c -- '*.rs'                              -> 10 files, +16,409 / -278
  git diff --numstat a6217328..08c476c -- '*.rs' docs/SELLER-QUICKSTART.md    -> 11 files, +16,558 / -293
  git diff -U0 a6217328..08c476c -- '*.rs' | grep -c '^@@'                    -> 176 hunks
  git diff --stat a6217328..HEAD | tail -1  (HEAD = reports head 3bbb1ee)     -> 197 files changed, 260,376 insertions(+), 293 deletions(-)
  (r9 body said "product files 11, +15,634 / -293" at 240240c; the verdict measured +16,029 at 1ee5cb2. Both are stale: the
   r10 code commits 1df95d8 / 5647417 / 98272ba / 31deed8 / 08c476c moved it to +16,558.)

exec-head delta
  git diff --numstat 31deed8..08c476c -- '*.rs'                               -> 1 file, +2 / -3  (fee_remit.rs record 45 assert literal)

census — added #[test] literals: see census-tests-08c476c.txt, TOTAL 83, every one past its file's test boundary.
  DISCREPANCY, named not hidden: the r9 body (¶104) reported 41 with a per-file split (fee_remit x18, lnurl_pay x5,
  platform_fee x1, run.rs x1, store.rs x6, seller_fees.rs x10). The same-shaped count at the r9 head reproduces neither the
  total nor the split: git diff -U0 a6217328..240240c -- '*.rs' | grep '^+' | grep -c '#\[test\]' -> 79 (fee_remit 40,
  lnurl_pay 14). The disagreement runs both ways (seller_fees.rs 10 in r9 vs 4 now), so it is not one method offset.
  r10 states the reproducible number with its command and flags the r9 row as unreproduced rather than restating it.
