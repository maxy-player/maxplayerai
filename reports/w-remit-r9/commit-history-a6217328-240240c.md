# #979 commit history `a6217328..240240c`, round by round — moved out of the pinned body (round 9, ruling 2026-09-08T18:32Z item 2)

Verbatim from body line 1 of `reports/w-remit-r9/body-r9.md` at `37b3615` (the per-round commit list) and body line 66 (the round-9 commits with what each did). Nothing rewritten. Executable head `240240c3f26bd35c3a8c5a3d70229f9730a0a221`; every commit after it is `reports/` only; the final head is the pinned body's commit, the pin commit records the read-back.

## Every commit, round by round (body line 1)

`3cda4ee` (brief), `0ac30e5` (addendum 1), `ebb4f30` (addendum 2, DENY), `a68511a` + `77cce72` (round 2, DENY), `3a9583e` `bca4df0` `9819de7` `99549a8` `2343a6c` `c0c5303` `534247b` (round 3, DENY), `881f508` `919c338` `13dd617` `7e1a7e3` `e09aa84` `7f03024` `455b8f8` `c64d39c` `5a3ce57` `098471c` `3adc47c` `9ee2d45` `1677e37` `02ca135` `6fc77e1` (round 4, DENY on B3), then round 5 (addendum 6): `16f0daa` (test fake mint in the CDK 0.17.2 shape: wallet half / melt gate / mint half that accepts UNPAID **or FAILED** with no expiry check), `a7eded2` (the hold: a bound spending row is held on everything but PAID), `1cab6f8` (the two decision tables and the interrupted-attempt test expect HOLD), `cbb3b5d` (2g b2 held not released, d's FAILED arm has the mint pay), `6cab9fc` (node 2b test holds on FAILED, test (a) reads the shared registry), `ede02c0` (the new delayed-confirm test), `bd369c5` (CLI `exit_code_for`, a held row exits REFUSED; one HELD line per run asserted), `0d6266d` (unused import), `c361878` (rustfmt over this round's lines), `6fd13df` (H prose), then round 6 (addendum 7): `ec56d0c` (§1: the delayed-confirm test exercises the STORE's refusal too), `edecd76` (fmt), `c7930c3` (H1: bound spending + PENDING/UNKNOWN is `SpendingHeld` — one HELD line for all five observations), `9c8ce6b` (H1: real CLI dry-run + `--confirm` invocation test), `dd73580` (fmt), `9bc76ce` (H2 prose), `19f30d3` (H3 source prose), then round 7 (addendum 8, after the DENY at `19f30d3`): `a16ac78` (B4: two-phase melt payment in `wallet_ops` — `MeltCeiling::admits_total`, `MeltTotalExceedsCeiling`, `PreparedMeltPayment` on a dedicated thread, `MeltEstimate::expected_fees_sats`), `d3fd3f6` (fmt), `877b3b8` (B4: `RemitEffects::{prepare_melt, confirm_melt, cancel_melt}`; `remit_inner` = quote → reserve check → prepare → total bound → fence → confirm), `6534290` (fmt), `c2b2293` (fake fee model + regressions (i)/(ii)), `9327138` (fmt), `f05345f` (fee-aware planning, `FeesDoNotFit`, regressions (iii-b) and never-fits), `09a6a93` (fmt), `f5c40cc` (H1: whole-stdout equality), `e97e18c` (H2: incident line), `2d802a3` (H3 source prose), `24ba082` (five clippy diagnostics on round-7 lines cleared — round 7's executable head), `da0ee92` (quickstart prose — round 7's final head, DENY on B/G/H: fee model), then round 8 (addendum 9): `4d5a5f3` (the fake's confirm made CDK-shaped: swap output split, recomputed input fee, post-swap refusal), `a7c5968` (§1.1 confirmability before the fence + §1.3 fake self-check against the confirm model), `1236efc` (§1.2 fee-aware planning chooses a genuinely confirmable invoice), `cd0322b` (§1.4 regressions on the CDK fee model), `8baaf3b` (§2: the SDK's inclusive `fee_paid` counted once; balance unknown, not computed), `060ebb7` (§3/§4.1: cancellation, recovery and retired-call prose said truthfully), `b1c702a` (§4.4/§1.5: module doc, `wallet_ops` doc and quickstart restated), `1a5c7c5` (rustfmt on our added lines, one constant assertion replaced — round 8's executable head), `036302b` (reports: every round-8 log, committed), `4714623` (round 8's body pin — round 8's final head, DENY on B/G/H: N1–N4), `ac033f4` (round 8's pin file), then round 9 (addendum 10): `b0c2c03` (plan), `c9cce6c` (A: one actual-confirmability bound, `admits_total` retired), `eefdb70` (B: planner and pre-fence check on `confirm_bound`), `b7e3f34` (C: `store.replan_remittance`), `573ab50` `c06b490` `1f03898` `bcbc8b9` (D: same-attempt re-plan when the live reserve shrank), `fa2b8af` (record 37 on the fee-metadata drift schedule), `e6f7b7a` (§1.4(a) test), `380f097` (§1.3 test), `9ae7c9c` `71b4971` (E: N2, row stays Planned, one line), `2521a44` (F: N3 labels, store fence doc), `240240c` (rustfmt on the round-9 test hunks, one clippy `identity_op` — **the executable head**), then reports-only commits (`4a1bd4a` … `a269952` and this body's pin — **the final head**)

## Round 9 commits, in order (body line 66)

**Round 9 commits, in order** (`git log --format=%s`; every one authored and committed as `w-seller-fee-stage2a-remit <worker@forge.local>`): `b0c2c03` (reports: the round-9 plan), `c9cce6c` (A: one actual-confirmability bound — `admits_confirmable`, `confirm_bound`, ppk before the gate, `MeltWouldNotConfirm`, `admits_total` retired), `eefdb70` (B: planner and pre-fence check on `confirm_bound`; prepared-total early return removed), `b7e3f34` (C: `store.replan_remittance`), `573ab50` → `c06b490` → `1f03898` → `bcbc8b9` (D: same-attempt re-plan; `fee_for` moved for `test_support`; re-plan only when the reserve **shrank**), `fa2b8af` (record 37 moves to the fee-metadata drift schedule), `e6f7b7a` (§1.4(a) test), `380f097` (§1.3 test), `9ae7c9c` → `71b4971` (E: N2 — `refuse_before_fence` writes nothing, one line; tests moved to the Planned contract), `2521a44` (F: N3 labels + the store fence doc — the intended executable head), `240240c` (rustfmt on the eight round-9 test hunks in `fee_remit.rs` and the three in `store.rs`'s replan test, one clippy `identity_op` in a test message — **the executable head**); between and after them the per-commit test logs and gate logs under `reports/w-remit-r9/` (`cb69791`, `c362d24`, `04ef5a1`, `3f406e2`, `e2983aa`, `8c49720`, `b1ef099`, `775f173`, `b1c9b1f`, `2bd8837`, `7382881`, `4a1bd4a`, `d0be4a0`, `49965f4`, `cec41be`, `2494a0a`, `c1909a5`, then this body's drafts). No `.rs`, `docs/` or `Cargo` change after `240240c` (diffstat below).
